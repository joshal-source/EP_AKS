# Copy to .env — see env.template for full comments
cp env.template .env

# Azure
AZURE_RESOURCE_GROUP=ep-rg
AZURE_LOCATION=eastus
AKS_CLUSTER_NAME=ep-aks
AKS_NODE_COUNT=3
AKS_NODE_VM_SIZE=Standard_D4s_v5

# Helm local overrides:
#   cp helm/edge-processor/values-local.yaml.example helm/edge-processor/values-local.yaml

# Deploy:
#   ./scripts/setup-from-install-script.sh install-script.txt --apply
